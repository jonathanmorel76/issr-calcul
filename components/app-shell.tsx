'use client'

import { useEffect, useMemo, useState } from 'react'
import IssrApp from '@/components/issr-app'
import BrandLogo from '@/components/brand-logo'
import DistanceCalculator from '@/components/distance-calculator'
import DashboardProfileHeader from '@/components/dashboard-profile-header'
import { isBirthday, useUserProfile } from '@/components/user-profile'
import { createClient } from '@/lib/supabase/client'
import { calcIndemKm, PRIME_REP_JOUR, PRIME_REPPLUS_JOUR, scheduleForDate, type IssrRateSchedule } from '@/lib/issr'
import { MISSION_NATURE_OPTIONS, missionNatureLabel, type MissionNature } from '@/lib/mission-nature'
import { TEACHER_STATUS_OPTIONS, teacherStatusLabel, type TeacherStatus } from '@/lib/teacher-status'

type EntrySummary={travel_date:string;distance_km:number;total_amount:number;rep_bonus:number;rep_plus_bonus:number}
type Establishment={id:string;name:string;address:string;is_rep:boolean;is_rep_plus:boolean;usual_distance_km:number|null;notes:string|null}
type Mission={id:string;title:string|null;start_date:string;end_date:string;status:string;establishment_id?:string|null;teacher_replacement_status:TeacherStatus|null;mission_nature:MissionNature|null;issr_establishments:{name:string}|null}
type View='dashboard'|'missions'|'indemnities'|'establishments'
type WidgetId='next-mission'|'attention'|'month-activity'|'indemnities'|'establishments'|'quick-actions'
type AttentionItem={title:string;detail:string;view:View;level:'warning'|'info'}
type Props={userId:string;initialEntries:EntrySummary[];initialMissions:Mission[];initialEstablishments:Establishment[];initialDefaultOrigin:string;rateSchedules:IssrRateSchedule[]}

const DEFAULT_WIDGETS:WidgetId[]=['next-mission','attention','month-activity','indemnities','establishments','quick-actions']
const WIDGET_LABELS:Record<WidgetId,string>={
 'next-mission':'Prochaine mission',attention:'À vérifier','month-activity':'Activité du mois',indemnities:'Indemnités du mois',establishments:'Mes établissements','quick-actions':'Accès rapides',
}
const WIDGET_STORAGE_KEY='mr-dashboard-widgets-v2'
function weekdaysBetween(start:string,end:string){const out:string[]=[];const d=new Date(start+'T12:00:00'),stop=new Date(end+'T12:00:00');while(d<=stop){const day=d.getDay();if(day!==0&&day!==6)out.push(d.toISOString().slice(0,10));d.setDate(d.getDate()+1)}return out}
function frDate(date:string){return new Date(date+'T12:00:00').toLocaleDateString('fr-FR',{weekday:'short',day:'2-digit',month:'short'})}
function ProductHero({title,description,action}:{title:string;description:string;action?:React.ReactNode}){return <header className="product-hero"><div className="product-hero-copy"><BrandLogo inverse/><div className="product-hero-text"><h1>{title}</h1><p>{description}</p></div></div>{action}</header>}

export default function AppShell({userId,initialEntries,initialMissions,initialEstablishments,initialDefaultOrigin,rateSchedules}:Props){
 const supabase=useMemo(()=>createClient(),[])
 const {profile}=useUserProfile()
 const displayName=profile?.display_name?.trim()||''
 const birthday=isBirthday(profile?.birth_date)
 const [view,setView]=useState<View>('dashboard'),[entries,setEntries]=useState(initialEntries),[missions,setMissions]=useState(initialMissions),[establishments,setEstablishments]=useState(initialEstablishments)
 const [name,setName]=useState(''),[address,setAddress]=useState(''),[rep,setRep]=useState(false),[repPlus,setRepPlus]=useState(false),[km,setKm]=useState('')
 const [estId,setEstId]=useState(''),[start,setStart]=useState(''),[end,setEnd]=useState(''),[title,setTitle]=useState(''),[message,setMessage]=useState('')
 const [missionTeacherStatus,setMissionTeacherStatus]=useState<TeacherStatus|''>(''),[missionNature,setMissionNature]=useState<MissionNature|''>('teacher_absence')
 const [defaultOrigin,setDefaultOrigin]=useState(initialDefaultOrigin),[generateDays,setGenerateDays]=useState(true),[busy,setBusy]=useState(false)
 const [schoolZone,setSchoolZone]=useState<'A'|'B'|'C'>('B'),[holidayDates,setHolidayDates]=useState<string[]>([]),[selectedDays,setSelectedDays]=useState<string[]>([]),[calendarLoading,setCalendarLoading]=useState(false)
 const [visibleWidgets,setVisibleWidgets]=useState<WidgetId[]>(DEFAULT_WIDGETS),[customizing,setCustomizing]=useState(false)
 const month=new Date().toISOString().slice(0,7),today=new Date().toISOString().slice(0,10),monthStart=`${month}-01`,monthEnd=`${month}-31`
 const monthEntries=entries.filter(e=>e.travel_date.startsWith(month)),monthMissions=missions.filter(m=>m.status!=='cancelled'&&m.start_date<=monthEnd&&m.end_date>=monthStart)
 const stats=monthEntries.reduce((a,e)=>({days:a.days+1,km:a.km+Number(e.distance_km),total:a.total+Number(e.total_amount),rep:a.rep+Number(e.rep_bonus)+Number(e.rep_plus_bonus)}),{days:0,km:0,total:0,rep:0})
 const nextMission=missions.filter(m=>m.end_date>=today&&m.status!=='cancelled').sort((a,b)=>a.start_date.localeCompare(b.start_date))[0]
 const selectedEst=establishments.find(e=>e.id===estId),candidateDays=useMemo(()=>start&&end&&end>=start?weekdaysBetween(start,end):[],[start,end]),holidaySet=useMemo(()=>new Set(holidayDates),[holidayDates])
 const attentionItems=useMemo<AttentionItem[]>(()=>{const items:AttentionItem[]=[];if(birthday)items.push({title:`🎂 Joyeux anniversaire${displayName?` ${displayName}`:''} !`,detail:'Mon Remplacement vous souhaite une très belle journée.',view:'dashboard',level:'info'});if(!establishments.length)items.push({title:'Aucun établissement configuré',detail:'Ajoutez votre premier établissement pour pouvoir planifier une mission.',view:'establishments',level:'info'});const missingDistance=establishments.filter(e=>e.usual_distance_km==null).length;if(missingDistance)items.push({title:`${missingDistance} établissement${missingDistance>1?'s':''} sans distance`,detail:'Calculez automatiquement la distance routière depuis votre établissement de rattachement administratif ou saisissez une valeur de référence.',view:'establishments',level:'warning'});const orphanMissions=missions.filter(m=>m.status!=='cancelled'&&(!m.establishment_id||!m.issr_establishments)).length;if(orphanMissions)items.push({title:`${orphanMissions} mission${orphanMissions>1?'s':''} à rattacher`,detail:'Un établissement lié à une mission est manquant ou n’est plus disponible.',view:'missions',level:'warning'});if(missions.length&&!defaultOrigin.trim())items.push({title:'Établissement de rattachement à compléter',detail:'Renseignez votre établissement de rattachement administratif avant de générer de nouvelles journées indemnisées.',view:'missions',level:'info'});return items},[birthday,displayName,establishments,missions,defaultOrigin])
 useEffect(()=>{try{const saved=window.localStorage.getItem(WIDGET_STORAGE_KEY);if(saved){const parsed=JSON.parse(saved) as WidgetId[];const valid=parsed.filter((x,i)=>DEFAULT_WIDGETS.includes(x)&&parsed.indexOf(x)===i);if(valid.length)setVisibleWidgets(valid)}}catch{}},[])
 useEffect(()=>{try{window.localStorage.setItem(WIDGET_STORAGE_KEY,JSON.stringify(visibleWidgets))}catch{}},[visibleWidgets])
 useEffect(()=>{if(profile?.teacher_status&&!missionTeacherStatus)setMissionTeacherStatus(profile.teacher_status)},[profile?.teacher_status,missionTeacherStatus])
 useEffect(()=>{if(!start||!end||end<start){setHolidayDates([]);setSelectedDays([]);return}const ctrl=new AbortController();setCalendarLoading(true);fetch(`/api/school-calendar?zone=${schoolZone}&start=${start}&end=${end}`,{signal:ctrl.signal}).then(r=>r.ok?r.json():Promise.reject(new Error('Calendrier indisponible'))).then(j=>{const blocked=(j.blocked_dates??[]) as string[];setHolidayDates(blocked);setSelectedDays(candidateDays.filter(d=>!blocked.includes(d)))}).catch(()=>setSelectedDays(candidateDays)).finally(()=>setCalendarLoading(false));return()=>ctrl.abort()},[start,end,schoolZone,candidateDays])
 const preview=useMemo(()=>{if(!selectedEst||selectedEst.usual_distance_km==null)return {days:selectedDays.length,total:0};const distance=Number(selectedEst.usual_distance_km);return selectedDays.reduce((a,date)=>{const schedule=scheduleForDate(rateSchedules,date);if(!schedule)return a;const mileage=calcIndemKm(distance,schedule),r=selectedEst.is_rep?PRIME_REP_JOUR:0,rp=selectedEst.is_rep_plus?PRIME_REPPLUS_JOUR:0;return {days:a.days+1,total:a.total+mileage+r+rp}},{days:0,total:0})},[selectedDays,selectedEst,rateSchedules])
 function toggleDay(date:string){setSelectedDays(v=>v.includes(date)?v.filter(d=>d!==date):[...v,date].sort())}
 function toggleWidget(id:WidgetId){setVisibleWidgets(v=>v.includes(id)?v.filter(x=>x!==id):[...v,id])}
 function moveWidget(id:WidgetId,direction:-1|1){setVisibleWidgets(v=>{const index=v.indexOf(id),target=index+direction;if(index<0||target<0||target>=v.length)return v;const next=[...v];[next[index],next[target]]=[next[target],next[index]];return next})}
 function resetWidgets(){setVisibleWidgets(DEFAULT_WIDGETS)}
 async function addEstablishment(){if(!name.trim()||!address.trim()){setMessage('Nom et adresse obligatoires.');return}if(defaultOrigin.trim()){const {error:settingsError}=await supabase.from('issr_user_settings').upsert({user_id:userId,default_origin:defaultOrigin.trim()},{onConflict:'user_id'});if(settingsError){setMessage(settingsError.message);return}}const {data,error}=await supabase.from('issr_establishments').insert({user_id:userId,name:name.trim(),address:address.trim(),is_rep:rep,is_rep_plus:repPlus,usual_distance_km:km?Number(km):null}).select().single();if(error){setMessage(error.message);return}setEstablishments(v=>[...v,data as Establishment].sort((a,b)=>a.name.localeCompare(b.name)));setName('');setAddress('');setKm('');setRep(false);setRepPlus(false);setMessage('Établissement enregistré.')}
 async function addMission(){if(!estId||!start||!end){setMessage('Choisissez un établissement et une période.');return}if(end<start){setMessage('La date de fin doit être postérieure au début.');return}if(generateDays&&selectedEst?.usual_distance_km==null){setMessage('Calculez ou renseignez d’abord la distance de cet établissement.');return}if(generateDays&&!defaultOrigin.trim()){setMessage('Renseignez votre établissement de rattachement administratif.');return}if(generateDays&&!selectedDays.length){setMessage('Sélectionnez au moins une journée travaillée.');return}setBusy(true);setMessage('');try{if(defaultOrigin.trim())await supabase.from('issr_user_settings').upsert({user_id:userId,default_origin:defaultOrigin.trim()},{onConflict:'user_id'});const effectiveTeacherStatus=missionTeacherStatus||profile?.teacher_status||null;const {data,error}=await supabase.from('issr_missions').insert({user_id:userId,establishment_id:estId,title:title.trim()||null,start_date:start,end_date:end,status:start<=today&&end>=today?'active':'planned',teacher_replacement_status:effectiveTeacherStatus,mission_nature:missionNature||null}).select('id,title,start_date,end_date,status,establishment_id,teacher_replacement_status,mission_nature,issr_establishments(name)').single();if(error)throw error;const mission=data as unknown as Mission;setMissions(v=>[...v,mission]);let generated:EntrySummary[]=[];if(generateDays&&selectedEst){const distance=Number(selectedEst.usual_distance_km);const rows=selectedDays.flatMap(date=>{const schedule=scheduleForDate(rateSchedules,date);if(!schedule)return [];const mileage=calcIndemKm(distance,schedule),repBonus=selectedEst.is_rep?PRIME_REP_JOUR:0,repPlusBonus=selectedEst.is_rep_plus?PRIME_REPPLUS_JOUR:0;return [{user_id:userId,mission_id:mission.id,generated_by_mission:true,travel_date:date,origin:defaultOrigin.trim(),destination:selectedEst.address,distance_km:distance,distance_source:'manual',is_rep:selectedEst.is_rep,is_rep_plus:selectedEst.is_rep_plus,mileage_allowance:mileage,rep_bonus:repBonus,rep_plus_bonus:repPlusBonus,total_amount:mileage+repBonus+repPlusBonus,rate_schedule_id:schedule.id,rate_code:schedule.code,rate_source_url:schedule.source_url}]});if(rows.length){const {data:created,error:entryError}=await supabase.from('issr_entries').insert(rows).select('travel_date,distance_km,total_amount,rep_bonus,rep_plus_bonus');if(entryError)throw entryError;generated=(created??[]) as EntrySummary[];setEntries(v=>[...generated,...v])}}setTitle('');setStart('');setEnd('');setEstId('');setMissionTeacherStatus(profile?.teacher_status??'');setMissionNature('teacher_absence');setHolidayDates([]);setSelectedDays([]);setMessage(generateDays?`Mission créée · ${generated.length} journée(s) ISSR générée(s).`:'Mission créée.')}catch(e:any){setMessage(e.message||'Impossible de créer la mission.')}finally{setBusy(false)}}
 async function removeEstablishment(id:string){const {error}=await supabase.from('issr_establishments').delete().eq('id',id);if(!error)setEstablishments(v=>v.filter(e=>e.id!==id));else setMessage('Cet établissement est peut-être encore lié à une mission.')}
 async function removeMission(id:string){setBusy(true);const {data:generated}=await supabase.from('issr_entries').select('travel_date').eq('mission_id',id);await supabase.from('issr_entries').delete().eq('mission_id',id).eq('generated_by_mission',true);const {error}=await supabase.from('issr_missions').delete().eq('id',id);if(!error){setMissions(v=>v.filter(m=>m.id!==id));const dates=new Set((generated??[]).map((x:any)=>x.travel_date));setEntries(v=>v.filter(e=>!dates.has(e.travel_date)))}setBusy(false)}
 const nav=<nav className="product-tabs"><button className={view==='dashboard'?'active':''} onClick={()=>setView('dashboard')}>Tableau de bord</button><button className={view==='establishments'?'active':''} onClick={()=>setView('establishments')}>Mes établissements</button><button className={view==='missions'?'active':''} onClick={()=>setView('missions')}>Mes missions</button><button className={view==='indemnities'?'active':''} onClick={()=>setView('indemnities')}>Mes indemnités</button><button disabled>Mes bilans</button><button disabled>Mes documents</button></nav>
 const prefix=displayName?`${displayName}, `:''
 if(view==='indemnities')return <div className="product-shell"><ProductHero title="Mes indemnités" description={`${prefix}suivez ce qui doit vous être versé, vérifiez les journées calculées et exportez vos justificatifs.`}/>{nav}<main className="dashboard-main indemnities-page"><div className="indemnities-embedded"><IssrApp userId={userId} embedded/></div></main></div>
 if(view==='establishments')return <div className="product-shell"><ProductHero title="Mes établissements" description={`${prefix}centralisez vos lieux de remplacement et laissez l’app calculer leur distance depuis votre établissement de rattachement administratif.`}/>{nav}<main className="dashboard-main"><section className="dashboard-panel"><h2>Ajouter un établissement</h2><div className="mr-form"><label>Nom<input value={name} onChange={e=>setName(e.target.value)} placeholder="École, collège, lycée…"/></label><label>Adresse<input value={address} onChange={e=>setAddress(e.target.value)} placeholder="Adresse complète"/></label><label className="mr-wide">Établissement de rattachement administratif<input value={defaultOrigin} onChange={e=>setDefaultOrigin(e.target.value)} placeholder="Adresse de votre établissement de rattachement"/><small>Cette adresse est mémorisée et réutilisée pour vos missions.</small></label><div className="mr-wide"><DistanceCalculator origin={defaultOrigin} destination={address} value={km} onChange={setKm}/></div><div className="mr-checks"><label><input type="checkbox" checked={rep} onChange={e=>{setRep(e.target.checked);if(e.target.checked)setRepPlus(false)}}/> REP</label><label><input type="checkbox" checked={repPlus} onChange={e=>{setRepPlus(e.target.checked);if(e.target.checked)setRep(false)}}/> REP+</label></div><button className="btn btn-primary" onClick={addEstablishment}>Enregistrer</button></div>{message&&<p className="mr-message">{message}</p>}</section><section className="dashboard-panel"><h2>Établissements enregistrés</h2><div className="mr-list">{establishments.length?establishments.map(e=><article key={e.id}><div><strong>{e.name}</strong><span>{e.address}</span><small>{e.is_rep_plus?'REP+':e.is_rep?'REP':'Hors REP'}{e.usual_distance_km!=null?` · ${e.usual_distance_km} km`:' · distance à calculer'}</small></div><button className="btn btn-danger" onClick={()=>removeEstablishment(e.id)}>Supprimer</button></article>):<div className="empty-state">Aucun établissement enregistré.</div>}</div></section></main></div>
 if(view==='missions')return <div className="product-shell"><ProductHero title="Mes missions" description={`${prefix}planifiez vos remplacements, choisissez les journées réellement travaillées et laissez l’app préparer l’estimation correspondante.`}/>{nav}<main className="dashboard-main"><section className="dashboard-panel"><h2>Créer une mission</h2><div className="mr-form"><label>Établissement<select value={estId} onChange={e=>setEstId(e.target.value)}><option value="">Sélectionner…</option>{establishments.map(e=><option key={e.id} value={e.id}>{e.name}</option>)}</select></label><label>Libellé (facultatif)<input value={title} onChange={e=>setTitle(e.target.value)} placeholder="Ex : remplacement CM2"/></label><label>Statut pour cette mission<select value={missionTeacherStatus} onChange={e=>setMissionTeacherStatus(e.target.value as TeacherStatus|'')}><option value="">Utiliser le statut du profil</option>{TEACHER_STATUS_OPTIONS.map(option=><option key={option.value} value={option.value}>{option.label}</option>)}</select><small>{profile?.teacher_status?`Par défaut : ${teacherStatusLabel(profile.teacher_status,true)}`:'Aucun statut principal renseigné dans Mon compte.'}</small></label><label>Nature de la mission<select value={missionNature} onChange={e=>setMissionNature(e.target.value as MissionNature|'')}><option value="">Non précisée</option>{MISSION_NATURE_OPTIONS.map(option=><option key={option.value} value={option.value}>{option.label}</option>)}</select><small>Ex. remplacement d’un enseignant absent, décharge de direction ou complément de temps partiel.</small></label><label>Du<input type="date" value={start} onChange={e=>setStart(e.target.value)}/></label><label>Au<input type="date" value={end} onChange={e=>setEnd(e.target.value)}/></label><label>Zone scolaire<select value={schoolZone} onChange={e=>setSchoolZone(e.target.value as 'A'|'B'|'C')}><option value="A">Zone A</option><option value="B">Zone B</option><option value="C">Zone C</option></select></label><label>Établissement de rattachement administratif<input value={defaultOrigin} onChange={e=>setDefaultOrigin(e.target.value)} placeholder="Adresse de votre établissement de rattachement"/></label><div className="mr-generate mr-wide"><label><input type="checkbox" checked={generateDays} onChange={e=>setGenerateDays(e.target.checked)}/> Générer automatiquement les indemnités pour les journées sélectionnées</label></div></div>{generateDays&&candidateDays.length>0&&<div className="mission-planner"><div className="planner-head"><div><strong>Jours travaillés</strong><span>{calendarLoading?'Vérification du calendrier scolaire…':`${selectedDays.length} sur ${candidateDays.length} jour(s) ouvré(s) retenu(s)`}</span></div><div><strong>{preview.total.toLocaleString('fr-FR',{style:'currency',currency:'EUR'})}</strong><span>estimation avant validation</span></div></div><div className="day-grid">{candidateDays.map(d=>{const holiday=holidaySet.has(d),checked=selectedDays.includes(d);return <label key={d} className={`${checked?'selected':''} ${holiday?'holiday':''}`}><input type="checkbox" checked={checked} onChange={()=>toggleDay(d)}/><span>{frDate(d)}</span>{holiday&&<small>Vacances scolaires</small>}</label>})}</div><p className="planner-source">Week-ends exclus automatiquement. Les vacances sont vérifiées via le calendrier officiel du ministère de l’Éducation nationale pour la zone {schoolZone}. Vous pouvez réactiver ou décocher chaque journée manuellement.</p></div>}<button className="btn btn-primary" onClick={addMission} disabled={!establishments.length||busy}>{busy?'Création…':'Créer la mission et générer les ISSR'}</button>{!establishments.length&&<p className="mr-message">Ajoutez d’abord un établissement : il sera ensuite réutilisable pour toutes vos missions.</p>}{message&&<p className="mr-message">{message}</p>}</section><section className="dashboard-panel"><h2>Missions enregistrées</h2><div className="mr-list">{missions.length?[...missions].sort((a,b)=>b.start_date.localeCompare(a.start_date)).map(m=><article key={m.id}><div><strong>{m.issr_establishments?.name??m.title??'Mission'}</strong><span>Du {new Date(m.start_date+'T12:00:00').toLocaleDateString('fr-FR')} au {new Date(m.end_date+'T12:00:00').toLocaleDateString('fr-FR')}</span><small>{[missionNatureLabel(m.mission_nature,true),teacherStatusLabel(m.teacher_replacement_status,true),m.status==='active'?'En cours':m.status==='completed'?'Terminée':'Planifiée'].filter(Boolean).join(' · ')}</small></div><button className="btn btn-danger" disabled={busy} onClick={()=>removeMission(m.id)}>Supprimer</button></article>):<div className="empty-state">Aucune mission enregistrée.</div>}</div></section></main></div>
 const dashboardAction=<DashboardProfileHeader defaultOrigin={defaultOrigin}/>
 const widgetContent=(id:WidgetId)=>{if(id==='next-mission')return <article key={id} className="dashboard-widget widget-next"><div className="widget-heading"><div><span className="eyebrow">À venir</span><h2>Prochaine mission</h2></div><span className="status-chip">Planning</span></div>{nextMission?<div className="next-mission"><strong>{nextMission.issr_establishments?.name??nextMission.title??'Mission'}</strong>{nextMission.mission_nature&&<small>{missionNatureLabel(nextMission.mission_nature)}{nextMission.teacher_replacement_status?` · ${teacherStatusLabel(nextMission.teacher_replacement_status,true)}`:''}</small>}<span>Du {new Date(nextMission.start_date+'T12:00:00').toLocaleDateString('fr-FR')} au {new Date(nextMission.end_date+'T12:00:00').toLocaleDateString('fr-FR')}</span><button className="widget-link" onClick={()=>setView('missions')}>Voir mes missions →</button></div>:<div className="empty-state"><strong>Aucune mission planifiée</strong><p>{establishments.length?'Vous pouvez planifier votre prochain remplacement dès maintenant.':'Commencez par enregistrer un établissement.'}</p><button className="widget-link" onClick={()=>setView(establishments.length?'missions':'establishments')}>{establishments.length?'Planifier une mission':'Ajouter un établissement'} →</button></div>}</article>;if(id==='attention')return <article key={id} className={`dashboard-widget widget-attention ${attentionItems.length?'has-items':'all-clear'}`}><div className="widget-heading"><div><span className="eyebrow">Pilotage</span><h2>À vérifier</h2></div><span className={`attention-count ${attentionItems.length?'warning':'ok'}`}>{attentionItems.length||'✓'}</span></div>{attentionItems.length?<div className="attention-list">{attentionItems.map((item,index)=><button key={`${item.title}-${index}`} className={`attention-item ${item.level}`} onClick={()=>setView(item.view)}><span className="attention-dot"/><span><strong>{item.title}</strong><small>{item.detail}</small></span><b>→</b></button>)}</div>:<div className="attention-clear"><span>✓</span><div><strong>Tout est à jour</strong><p>Aucun élément bloquant détecté dans vos établissements ou vos missions.</p></div></div>}</article>;if(id==='month-activity')return <article key={id} className="dashboard-widget widget-activity"><div className="widget-heading"><div><span className="eyebrow">Ce mois-ci</span><h2>Mon activité</h2></div></div><div className="activity-metrics"><div><strong>{stats.days}</strong><span>jours indemnisés</span></div><div><strong>{monthMissions.length}</strong><span>missions</span></div><div><strong>{stats.km.toLocaleString('fr-FR',{maximumFractionDigits:1})}</strong><span>km enregistrés</span></div></div></article>;if(id==='indemnities')return <article key={id} className="dashboard-widget widget-money"><div className="widget-heading"><div><span className="eyebrow">Suivi financier</span><h2>Indemnités du mois</h2></div></div><strong className="widget-amount">{stats.total.toLocaleString('fr-FR',{style:'currency',currency:'EUR'})}</strong><p>{stats.days} jour(s) enregistré(s) · dont {stats.rep.toLocaleString('fr-FR',{style:'currency',currency:'EUR'})} de REP / REP+</p><button className="widget-link" onClick={()=>setView('indemnities')}>Contrôler mes indemnités →</button></article>;if(id==='establishments')return <article key={id} className="dashboard-widget widget-establishments"><div className="widget-heading"><div><span className="eyebrow">Référentiel</span><h2>Mes établissements</h2></div></div><strong className="widget-count">{establishments.length}</strong><p>{establishments.length?'lieu(x) prêt(s) à être réutilisé(s) dans vos missions':'Aucun établissement enregistré pour le moment'}</p><button className="widget-link" onClick={()=>setView('establishments')}>{establishments.length?'Gérer mes établissements':'Ajouter mon premier établissement'} →</button></article>;return <article key={id} className="dashboard-widget quick-panel widget-quick"><div><span className="eyebrow">Accès rapide</span><h2>Actions fréquentes</h2></div><button onClick={()=>setView('establishments')}><span>🏫</span><div><strong>Établissements</strong><small>Ajouter ou mettre à jour un lieu</small></div><b>→</b></button><button onClick={()=>setView('missions')} disabled={!establishments.length}><span>📅</span><div><strong>Nouvelle mission</strong><small>{establishments.length?'Planifier un remplacement':'Ajoutez d’abord un établissement'}</small></div><b>→</b></button><button onClick={()=>setView('indemnities')}><span>€</span><div><strong>Indemnités</strong><small>Vérifier les montants du mois</small></div><b>→</b></button></article>}
 return <div className="product-shell"><ProductHero title="Votre espace de remplacement" description={displayName?`Bonjour ${displayName}. Retrouvez en un coup d’œil ce qui compte aujourd’hui : vos prochaines missions, votre activité du mois et les éléments à suivre.`:'Retrouvez en un coup d’œil ce qui compte aujourd’hui : vos prochaines missions, votre activité du mois et les éléments à suivre.'} action={dashboardAction}/>{nav}<main className="dashboard-main"><div className="dashboard-toolbar"><button className="btn btn-secondary" onClick={()=>setCustomizing(v=>!v)}>⚙ Personnaliser</button><button className="btn btn-primary" onClick={()=>setView(establishments.length?'missions':'establishments')}>{establishments.length?'Planifier un remplacement':'Configurer mes établissements'}</button></div>{customizing&&<section className="widget-customizer"><div className="widget-customizer-copy"><strong>Personnaliser mon tableau de bord</strong><p>Affichez uniquement les widgets utiles et réorganisez-les selon vos priorités. L’ordre est enregistré sur cet appareil.</p><button className="widget-reset" onClick={resetWidgets}>Réinitialiser l’affichage</button></div><div className="widget-toggles">{DEFAULT_WIDGETS.map(id=>{const active=visibleWidgets.includes(id),index=visibleWidgets.indexOf(id);return <div className={`widget-toggle-row ${active?'active':''}`} key={id}><label><input type="checkbox" checked={active} onChange={()=>toggleWidget(id)}/><span>{WIDGET_LABELS[id]}</span></label><div className="widget-order-actions"><button aria-label={`Monter ${WIDGET_LABELS[id]}`} disabled={!active||index<=0} onClick={()=>moveWidget(id,-1)}>↑</button><button aria-label={`Descendre ${WIDGET_LABELS[id]}`} disabled={!active||index<0||index>=visibleWidgets.length-1} onClick={()=>moveWidget(id,1)}>↓</button></div></div>})}</div></section>}<section className="widget-grid">{visibleWidgets.map(widgetContent)}</section></main></div>
}
